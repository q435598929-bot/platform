# task-server

Java 21 + Spring Boot 任务管理服务。原 `task-executor` 业务类完整保留，仍可以
直接运行各类的 `main()`；服务层通过同一入口提供任务登记、启停开关、确认执行、
全局单任务锁、执行历史和平台日志。

所有兼容任务默认禁用。服务化入口不会改写原任务的 SDK 地址、请求字段、重试和
Excel 读写逻辑。

两个依赖旧版 SDK `demo.*` 辅助类的和兆示例保存在 `legacy-sources`，不参与
服务编译或白名单执行；它们不是正式业务任务。
